# 根据 Farcaster 文档修复的问题

根据 [Farcaster Mini Apps 发布文档](https://miniapps.farcaster.xyz/docs/guides/publishing)，我发现并修复了以下问题：

## 🔍 发现的问题

### 1. 图标尺寸不符合要求 ❌

**文档要求**：
- `iconUrl`: Image must be **1024x1024px PNG, no alpha**

**之前**：
- 512x512 PNG，有 alpha 通道（RGBA）

**已修复**：
- ✅ 生成 1024x1024 PNG：`neynar-score-icon-1024.png`
- ✅ 更新 manifest 中的 `iconUrl`

### 2. 启动画面尺寸不符合要求 ❌

**文档要求**：
- `splashImageUrl`: Must be **200x200px**

**之前**：
- 1024x1024 PNG

**已修复**：
- ✅ 生成 200x200 PNG：`splash-screen-200.png`
- ✅ 更新 manifest 中的 `splashImageUrl`

### 3. subtitle 长度超过限制 ❌

**文档要求**：
- `subtitle`: Max **30 characters**, no emojis or special characters

**之前**：
- "Check Farcaster user score instantly." (37 字符)

**已修复**：
- ✅ "Check user score instantly" (26 字符)

### 4. tags 数量超过限制 ❌

**文档要求**：
- `tags`: Up to **5 tags**, max 20 characters each

**之前**：
- 6 个标签：["neynar", "score", "analytics", "profile", "utility", "farcaster"]

**已修复**：
- ✅ 5 个标签：["neynar", "score", "analytics", "profile", "utility"]

## ✅ 修复后的配置

### 图片文件

| 文件 | 尺寸 | URL |
|------|------|-----|
| 图标 | 1024x1024 | `/neynar-score-icon-1024.png` |
| 启动画面 | 200x200 | `/splash-screen-200.png` |

### 字段验证

- ✅ `subtitle`: 26/30 字符
- ✅ `tags`: 5/5 个
- ✅ `iconUrl`: 指向 1024x1024 图片
- ✅ `splashImageUrl`: 指向 200x200 图片

## 📋 文档要求总结

根据文档，以下是关键要求：

### 必需字段

- ✅ `version`: "1"
- ✅ `name`: Max 32 characters
- ✅ `homeUrl`: Max 1024 characters
- ✅ `iconUrl`: 1024x1024px PNG, no alpha

### 可选字段限制

- `subtitle`: Max 30 characters, no emojis or special characters
- `description`: Max 170 characters, no emojis or special characters
- `tagline`: Max 30 characters
- `ogTitle`: Max 30 characters
- `ogDescription`: Max 100 characters
- `tags`: Up to 5 tags, max 20 characters each, lowercase, no spaces
- `splashImageUrl`: Must be 200x200px

## 🚀 下一步

1. **等待部署完成**（约 2-3 分钟）

2. **验证图片可访问**：
   ```
   https://farcaster-neynar-score-miniapp.vercel.app/neynar-score-icon-1024.png
   https://farcaster-neynar-score-miniapp.vercel.app/splash-screen-200.png
   ```

3. **验证 manifest 已更新**：
   ```
   https://farcaster-neynar-score-miniapp.vercel.app/.well-known/farcaster.json
   ```

4. **在 Farcaster 创建页面重新提交**

## 📝 注意事项

- 图标必须是 1024x1024 PNG，无透明通道（no alpha）
- 启动画面必须是 200x200px
- 所有字符串字段都有长度限制
- tags 最多 5 个，每个最多 20 字符，小写，无空格

